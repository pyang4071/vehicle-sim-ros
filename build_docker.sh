# build docker contianer
# t [tag] flag - build the docker contianer with a name; in this case `vehicle-dev`
# the final . means to use the current repo as the context

sudo docker build -t vehicle-dev .
